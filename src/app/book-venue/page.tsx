"use client";
import { BookVenueView } from "@/features";
import { NextPageWithLayout } from "@/types";

const About: NextPageWithLayout = () => {
  return <BookVenueView />;
};

export default About;
